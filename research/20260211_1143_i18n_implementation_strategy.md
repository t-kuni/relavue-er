## 1. ライブラリ選定（意思決定）

### 選択肢A: 多言語対応ライブラリを導入する

#### A-1) **react-i18next（react-i18next v16系 + i18next v25系）**

* **バージョン**: react-i18next v16.5.4、i18next v25.8.4 ([NPM][1])
* **メリット**

  * UIテキスト中心のi18nに強い（キー→翻訳の素直なモデル、分割/名前空間、補間、複数形など）。
  * ブラウザ言語の検出プラグインがある（i18next-browser-languageDetector）。([GitHub][2])
  * “後で増える”前提（画面/機能増）に耐えやすい。
* **デメリット**

  * 依存が増える（react-i18next + i18next 本体）。
  * 文字列抽出/翻訳管理の運用は別途設計が必要（JSON直編集のままだと破綻しやすい）。
* **MVP観点**

  * 「まず3言語」「UIラベル/ボタン中心」なら導入コスパが高い。
  * ただし**ViewModelを言語の単一ソース**にしたいなら、i18next側の永続化（localStorage等）を使いすぎない設計が必要。

#### A-2) **react-intl（FormatJS / react-intl v8系）**

* **バージョン**: react-intl v8.1.3 ([NPM][3])
* **メリット**

  * ICUメッセージ（複数形/セレクト/日付数値の表現）を体系的に扱える。([formatjs.github.io][4])
  * 「文章が長い」「表現が複雑」になったときに破綻しにくい。
* **デメリット**

  * UIラベル中心でも導入自体は可能だが、メッセージ記述が重めになりがち（MVPでは過剰になりやすい）。
  * 周辺依存や学習コストが上がりやすい（ICU記法をチームで揃える必要）。
* **MVP観点**

  * “文章中心のアプリ”なら強いが、ER図ツールの「短いUIテキスト中心」ではオーバーになりやすい。

#### A-3) **Lingui（@lingui/core / @lingui/react v5系）**

* **バージョン**: v5.9.0（Changelog/NPM）([GitHub][5])
* **メリット**

  * 抽出・カタログ運用のワークフローを含めやすい（規模が伸びたときの運用が楽になりやすい）。([lingui.dev][6])
  * Vite/Reactでの利用前提が明確。
* **デメリット**

  * MVP段階では導入・運用設計（抽出/更新/翻訳）まで含めるとやや重い。
* **MVP観点**

  * “最初から翻訳運用を整えたい”なら有力。軽さ優先なら過剰になり得る。

#### （参考）バンドルサイズ感

* react-i18next + i18next は合計で概ね 20KB台（min+gzip目安）、react-intl は 10KB台後半（min+gzip目安）という比較が紹介されている。([i18nexus][7])
  （※ビルド条件で変動するため、最終判断は実測推奨）

---

### 選択肢B: 自前で実装する

* **メリット**

  * 依存ゼロで最小実装にできる（辞書 + `t(key)` + locale切替）。
  * 既存の「自前Store + useSyncExternalStore」と整合しやすい（localeが変われば購読で再描画）。
* **デメリット**

  * 複数形、フォールバック、キー管理、未翻訳検知、抽出、翻訳運用などを後から欲しくなると再実装が増える。
* **MVP観点**

  * “当面UIテキストが少ない”なら最速。ただし増えた瞬間にツケが出やすい。

---

## 2. ViewModelへの言語設定の追加方法（意思決定）

### 選択肢A: ViewModel直下に追加（例: `ViewModel.locale`）

* **メリット**: 最短で分かりやすい（どこからでも見える）。
* **デメリット**: 設定が増えるとルートが肥大化しやすい。

**TypeSpec例**

```tsp
alias Locale = "ja" | "en" | "zh";

model ViewModel {
  format: string;
  version: int32;
  locale?: Locale;
  erDiagram: ERDiagramViewModel;
  ui: GlobalUIState;
  buildInfo: BuildInfoState;
  settings?: AppSettings;
}
```

### 選択肢B: AppSettings内に追加（例: `ViewModel.settings.locale`）

* **メリット**: “アプリ設定”としてまとまりが良い（将来の設定増に耐える）。
* **デメリット**: `settings` が optional の場合、生成/マージ処理が少し増える。

**TypeSpec例**

```tsp
alias Locale = "ja" | "en" | "zh";

model AppSettings {
  locale?: Locale;
  // 既存の設定があればここに追加していく
}

model ViewModel {
  format: string;
  version: int32;
  erDiagram: ERDiagramViewModel;
  ui: GlobalUIState;
  buildInfo: BuildInfoState;
  settings?: AppSettings;
}
```

---

## 3. ブラウザの言語設定の検出方法

### JavaScriptでの取得

* 優先言語リスト: `navigator.languages` ([MDNウェブドキュメント][8])
* 単一の優先言語: `navigator.language` ([MDNウェブドキュメント][9])

例（`navigator.languages` → 対応言語にマッチ → フォールバック）:

```ts
const supported = ["ja", "en", "zh"] as const;
type Locale = (typeof supported)[number];

function normalize(tag: string): string {
  // "ja-JP" -> "ja", "zh-Hans-CN" -> "zh" など（最小）
  return tag.toLowerCase().split("-")[0];
}

export function detectLocale(fallback: Locale = "en"): Locale {
  const candidates = (navigator.languages?.length ? navigator.languages : [navigator.language]).filter(Boolean);
  for (const tag of candidates) {
    const base = normalize(tag);
    if ((supported as readonly string[]).includes(base)) return base as Locale;
  }
  return fallback;
}
```

### フォールバック戦略

* 基本は「ユーザーの明示設定が最優先 → 次に保存済み設定 → 最後にブラウザ設定でネゴシエーション」という考え方が推奨される。([DEV Community][10])
  （本件では“保存済み”が ViewModel の locale）

---

## 4. 言語切り替えUIの設計（意思決定）

### 選択肢A: ヘッダーにドロップダウン

* **評価（ER図ビューア特性込み）**

  * ヘッダーが既にボタン多めの場合、ラベル付きドロップダウンは横幅を取りやすい。
  * ただし最も分かりやすく、発見性が高い。

### 選択肢B: 設定モーダル内に配置

* **評価**

  * ヘッダー混雑を避けられる。
  * 反面、切替頻度が高いと不便（1ステップ増）。

### 選択肢C: ヘッダーにアイコンのみ（クリックで言語メニュー）

* **評価**

  * 省スペースでヘッダーを崩しにくい（このアプリに相性が良い）。
  * 意味が伝わりにくい懸念はあるが、一般的な「🌐/A文」系アイコン + ツールチップで軽減可能。

---

## 5. 翻訳対象の洗い出し（概算）

現状説明から推定される対象（代表例）:

* **ヘッダー**: エクスポート/インポート/レイヤー/履歴/ビルド情報/DB接続/ヘルプ等（約10〜20）
* **サイドパネル**: タイトル、タブ名、セクション見出し、ラベル（約20〜40）
* **モーダル**: タイトル、説明文、フォームラベル、ボタン、バリデーション文（約25〜60）
* **エラー/トースト**: API失敗、ファイル形式不正、バリデーション失敗など（約15〜40）
* **空状態/ヒント**: “選択してください”“ドラッグ…”等（約10〜25）

**合計見積もり**: 約80〜185文字列
（実数はコード内のUI文字列数に依存）

---

## 6. エクスポート・インポートでの言語設定の扱い

### エクスポート

* locale は **保持する**（要件通り“設定の一部”として出す）。
* 既存の「一時UI状態とキャッシュ初期化」の対象から locale を外す（消さない）。

### インポート

* インポートファイルに locale があれば **その値で復元**。
* locale が無い（旧ファイル等）場合は **ブラウザ検出→フォールバック**で補完。

### バリデーション

* locale が存在する場合は `"ja" | "en" | "zh"` のいずれかをチェック。
* 不正値なら（1）無視して検出結果を採用、（2）フォールバック固定、のどちらかに統一。

---

## 7. TypeSpecでの型定義への影響

### 型定義例（共通）

```tsp
alias Locale = "ja" | "en" | "zh";
```

### 追加先

* 2章の **A（直下）** or **B（settings）** のどちらか。

### バックエンドAPIへの影響（`GET /api/init`）

* ブラウザ言語はサーバ側で確定できないため、`/api/init` が locale を返す必然性は薄い。
* ただし「ViewModelにlocaleフィールドが存在する」前提なら、`/api/init` は **locale未設定（undefined）** の初期ViewModelを返し、フロント初期化で `detectLocale()` 結果をStoreへ反映する形が自然。
* もしサーバ側でやるなら `Accept-Language` を使うのが一般的。([DEV Community][10])

---

## 8. 実装の優先順位と段階的アプローチ（意思決定）

### 選択肢A: 一度に全て実装

* **メリット**: 仕様の一貫性が高い（未翻訳が混ざりにくい）。
* **デメリット**: MVP速度が落ちる。

### 選択肢B: 言語を段階追加（最初は日英、後で中）

* **メリット**: 早く出せる。
* **デメリット**: 追加時に翻訳漏れ・テスト漏れが出やすい（仕組みは最初に完成させる必要あり）。

### 選択肢C: 画面/領域を段階対応（主要UIのみ→全体）

* **メリット**: もっと早く出せる。
* **デメリット**: “一部だけ翻訳済み”の体験になりやすい（プロダクトの印象に影響）。

---

## 9. パフォーマンスとバンドルサイズへの影響

### 翻訳ファイル配置

* **3言語・UIテキスト程度**なら、最初はバンドル同梱で十分なことが多い（単純）。
* 将来、文言や言語が増えるなら **動的ロード**（言語切替時にカタログを `import()` / HTTPロード）へ移行。

  * i18next はバックエンド/フォールバック構成が用意されている。([i18next.com][11])
  * Lingui も lazy loading 等の拡張を前提にしている。([lingui.dev][6])

### ライブラリ導入時の影響

* react-i18next は i18next 本体込みになるため、react-intlより増えやすいという比較がある。([i18nexus][7])
  （ただし差分は大きすぎるとは限らない、最終は実測）

### 実行時パフォーマンス

* 主要コストは「言語切替で再描画が走る」点。設計としては

  * locale変更時だけ必要コンポーネントが再描画される（Store購読粒度）
  * 翻訳関数が無駄に毎回重い処理をしない
    が重要。

---

## 10. 他のER図/DB可視化ツールの多言語対応例（参考）

* **DBeaver**: Preferences（設定）でUI言語をドロップダウン選択。([DBeaver][12])
* **pgAdmin 4**: Preferencesで“User language”を選んで表示言語を変更。([pgadmin.org][13])
* **diagrams.net（draw.io）**: Settings > Language で言語変更（リロードが必要）。([Draw.io][14])
* **DbSchema**: UI国際化があり、設定ダイアログから再設定可能。([dbschema.com][15])

---

## 11. 推奨される実装方針（推奨度順）

### 提案1（推奨）: **react-i18next（v16系）+ i18next（v25系）**

* **使用ライブラリ**: react-i18next v16.5.4 + i18next v25.8.4 ([NPM][1])
* **ViewModel組み込み**: `ViewModel.settings.locale?: "ja" | "en" | "zh"`（2章B）
* **UI設計**: 選択肢C（ヘッダーにアイコン、クリックでメニュー）
* **段階的アプローチ**: 8章B（まず日英で運用を固め、中は追加）
* **メリット**: MVPで実用性が高く、後で増えても破綻しにくい
* **デメリット**: 依存追加、i18next側状態とViewModelの二重管理にならないよう設計が必要
* **実装の複雑さ**: 中（ただし“仕組みの完成度”は高い）

### 提案2: **Lingui（v5系）**

* **使用ライブラリ**: @lingui/core / @lingui/react v5.9.0 ([GitHub][5])
* **ViewModel組み込み**: `settings.locale`
* **UI設計**: 選択肢C or B（設定に寄せても良い）
* **段階的アプローチ**: 8章B
* **メリット**: 翻訳運用（抽出/カタログ）を早期に整えやすい ([lingui.dev][6])
* **デメリット**: MVP段階だと運用設計まで含めて重くなりやすい
* **実装の複雑さ**: 中〜高

### 提案3: **自前実装（最小）**

* **使用ライブラリ**: なし（辞書 + `t(key)` + `Intl`）
* **ViewModel組み込み**: `settings.locale`
* **UI設計**: 選択肢C
* **段階的アプローチ**: 8章B or C（主要UIから）
* **メリット**: 最速・最小依存で今のStore設計に自然に乗る
* **デメリット**: 翻訳対象が増えるほど後から移行/拡張のコストが出やすい
* **実装の複雑さ**: 低（ただし将来の保守コストは増えがち）

[1]: https://www.npmjs.com/package/react-i18next?utm_source=chatgpt.com "react-i18next"
[2]: https://github.com/i18next/i18next-browser-languageDetector?utm_source=chatgpt.com "language detector used in browser environment for i18next"
[3]: https://www.npmjs.com/package/react-intl?utm_source=chatgpt.com "react-intl"
[4]: https://formatjs.github.io/docs/react-intl/?utm_source=chatgpt.com "React Intl"
[5]: https://github.com/lingui/js-lingui/blob/main/packages/react/CHANGELOG.md?utm_source=chatgpt.com "js-lingui/packages/react/CHANGELOG.md at main"
[6]: https://lingui.dev/?utm_source=chatgpt.com "Lingui"
[7]: https://i18nexus.com/posts/comparing-react-i18next-and-react-intl?utm_source=chatgpt.com "react-i18next vs react-intl"
[8]: https://developer.mozilla.org/en-US/docs/Web/API/Navigator/languages?utm_source=chatgpt.com "Navigator: languages property - Web APIs | MDN"
[9]: https://developer.mozilla.org/en-US/docs/Web/API/Navigator/language?utm_source=chatgpt.com "Navigator: language property - Web APIs | MDN"
[10]: https://dev.to/lingodotdev/every-way-to-detect-a-users-locale-from-best-to-worst-369i?utm_source=chatgpt.com "Every way to detect a user's locale (from best to worst)"
[11]: https://www.i18next.com/how-to/backend-fallback?utm_source=chatgpt.com "Backend Fallback"
[12]: https://dbeaver.com/docs/dbeaver/UI-Language/?utm_source=chatgpt.com "Interface language | DBeaver Documentation"
[13]: https://www.pgadmin.org/docs/pgadmin4/8.14/preferences.html?utm_source=chatgpt.com "Preferences Dialog — pgAdmin 4 8.14 documentation"
[14]: https://www.drawio.com/doc/faq/menu-language-change?utm_source=chatgpt.com "Change the editor interface and menu language - draw.io"
[15]: https://dbschema.com/blog/release-notes/release-notes9/?srsltid=AfmBOorGfPgPH_OMmXJmllqEb9RTRRCv46cKxyhdzpt1UmECcS9mWqqW&utm_source=chatgpt.com "DbSchema 9.9.4 Release Notes – New Features and ..."
